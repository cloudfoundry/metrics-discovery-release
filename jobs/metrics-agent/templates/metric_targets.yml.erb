<%
target = {
  "targets" => [
    "#{spec.ip}:#{p('metrics_exporter_port')}"
  ],
  "labels" => {
    "job" => "metrics_agent_exporter_#{spec.name}",
  },
  "source" => "metrics_agent_exporter_#{spec.ip}",
}
%>

<%= YAML.dump(target) %>