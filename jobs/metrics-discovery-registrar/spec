---
name: metrics-discovery-registrar

consumes:
- name: nats
  type: nats

templates:
  bpm.yml.erb: config/bpm.yml

packages:
  - metrics-discovery-registrar

properties:
  routes:
    description: "A comma separated list of metrics endpoints to publish for scraping"
  publish_interval:
    description: "interval to publish routes"
    default: 15s
