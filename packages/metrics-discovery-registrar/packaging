set -ex

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.12.7)
export PATH=$GOROOT/bin:$PATH
export HOME=/var/vcap

go build -mod=vendor -o ${BOSH_INSTALL_TARGET}/discovery-registrar ./cmd/discovery-registrar
